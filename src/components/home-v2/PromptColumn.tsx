'use client'

// PromptColumn is no longer used — prompt cards are rendered inline in home-v2/page.tsx
// Kept as empty export for import compatibility
export function PromptColumn() {
  return null
}
